let colors = ['blue', 'black', 'red', 'green', 'orange'];

// let index = 0;
// while (index < colors.length) {
//     let num = index + 1;
//     let color = colors[index];
//     console.log('My #' + num + ' choice is ' + color);

//     index = index + 1;
// }

for (let index = 0; index < colors.length; index = index + 1) {
    let num = index + 1;
    let color = colors[index];
    console.log('My #' + num + ' choice is ' + color);
}
